原因はシグナルはキューに入れられないため。
子プロセスから親プロセスへ計5回シグナル SIGUSR2 が送られる。最初のシグナルはハンドラに処理され、処理中にハンドラは二番目のシグナルを受信する。二番目のシグナルはペンディングされたシグナルの集合に加えられる。その後すぐに三番目のシグナルを受信するがすでにペンディングされた SIGUSR2 シグナルが存在するため捨てられる。四、五番目のシグナルも同様に捨てられる。その後ハンドラから処理が戻ると、カーネルはペンディングになっている SIGUSR2 シグナルがあることを通知し、二番目のシグナルを処理する。よってカウンタの値は 2 になる。
